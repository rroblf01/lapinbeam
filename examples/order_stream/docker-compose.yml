services:
  postgres:
    image: postgres:16-alpine
    container_name: order_stream_postgres
    environment:
      - POSTGRES_USER=warehouse
      - POSTGRES_PASSWORD=warehouse
      - POSTGRES_DB=orders
    networks:
      - order-net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U warehouse -d orders"]
      interval: 2s
      timeout: 2s
      retries: 20
    # No named volume on purpose: this is a throwaway demo/benchmark
    # environment, torn down and rebuilt between test runs.
    tmpfs:
      - /var/lib/postgresql/data

  worker:
    build: ./worker
    container_name: order_stream_worker
    environment:
      - NODE_NAME=worker@worker:9001
      - DATABASE_URL=postgresql://warehouse:warehouse@postgres:5432/orders
      - MAX_PARALLEL=${MAX_PARALLEL:-200}
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - order-net
    # NODE_NAME's host is also the interface the listener binds to, so
    # the healthcheck has to dial that same name, not `localhost` (see
    # docs/index.md's "Security" note in the main project).
    healthcheck:
      test: ["CMD", "python", "-c", "import socket; socket.create_connection(('worker', 9001), timeout=2)"]
      interval: 2s
      timeout: 2s
      retries: 30

  app:
    build: ./app
    container_name: order_stream_app
    environment:
      - NODE_NAME=app@app:9000
      - WORKER_NODE=worker@worker:9001
      - DATABASE_URL=postgresql://warehouse:warehouse@postgres:5432/orders
    ports: ["8000:8000"]
    depends_on:
      postgres:
        condition: service_healthy
      worker:
        condition: service_healthy
    networks:
      - order-net

networks:
  order-net:
    driver: bridge
